[
  {
    "id": "TV-013",
    "module": "trust-vault",
    "title": "No SLA or deadline visible on review items in reviewer queue — `MyReviewsTab` shows title/status/assigned date only; no due date, no overdue flag; reviewer with 30 items has no urgency prioritisation; violates ISO 19011:2018 §5.5",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED — verified 2026-09-02, full end-to-end trace. AssignReviewerModal.jsx has a real date input (dueDate); certification.routes.ts's transitionSchema accepts it; CertificationService.transition/ReportService.transition thread it into WorkflowTransitionService as opts.metadata.dueDate, which the workflow engine persists onto the WorkflowEntry; workflow.routes.ts's my-reviews aggregate now projects dueDate for both cert and report items; MyReviewsTab.jsx renders a new 'Due' column with OVERDUE/Due-in-Nd badges.",
    "category": "Workflow Integrity",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/MyReviewsTab.jsx\n\nRoot Cause: `MyReviewsTab` shows title, status, and assigned date only. No due date, no overdue flag, no SLA indicator. A reviewer with 30 items has no urgency basis for prioritisation.\n\nClaude Fix Prompt: Add SLA column: `<td>{review.dueDate ? (<span className={isPast(review.dueDate) ? 'text-destructive font-medium' : ''}>{isPast(review.dueDate) ? 'OVERDUE' : \\`Due in \\${differenceInDays(review.dueDate, new Date())}d\\`}</span>) : <span className=\"text-muted-foreground\">No deadline</span>}</td>`. Sort queue by `dueDate` ascending by default.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "requirement": "ISO 19011:2018 §5.5",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`AssignReviewerModal.jsx` (dueDate input) → `certification.routes.ts`/`workflow.routes.ts` (schema + my-reviews projection) → `CertificationService.ts`/`ReportService.ts` (opts.metadata.dueDate) → core workflow engine (spreads opts.metadata onto the persisted entry) → `MyReviewsTab.jsx` (Due column, OVERDUE badge) — every link in the chain confirmed present and correctly wired on 2026-09-02 (client-panel HEAD 0b8712ce, trust-vault-service HEAD 12b4103). No regression found; queue is not yet sorted by dueDate ascending by default (a minor residual UX gap versus the fix prompt, not a functional one)."
  },
  {
    "id": "TV-014",
    "module": "trust-vault",
    "title": "No per-access audit trail of who accessed a share link — `ShareLinkModal` shows `viewCount` only; no timestamp, recipient identifier, or IP per access event; violates ISO 27001:2022 Annex A 5.14",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "Already fixed in the live code — ShareLinkModal.jsx has a full Access History feature (per-access log with timestamp/IP/recipient, expandable per link, CSV export for audit submission). No code change was needed; verified by reading the current file.",
    "category": "Audit Trail",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/ShareLinkModal.jsx\n\nRoot Cause: Share link modal shows `viewCount` total only. No per-access record with timestamp, IP address, or recipient identifier is stored or displayed.\n\nClaude Fix Prompt: Backend: on every share link access, append to `ShareLinkAccessLog`: `{linkId, accessedAt: new Date(), ipAddress: req.ip, userAgent: req.headers['user-agent']}`. Frontend `ShareLinkModal`: add `<Collapsible><CollapsibleTrigger>View access log ({accessLog.length} entries)</CollapsibleTrigger><CollapsibleContent>{accessLog.map(e => <div>{formatDateTime(e.accessedAt)} — {e.ipAddress}</div>)}</CollapsibleContent></Collapsible>`.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-06-12",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Annex A 5.14",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md"
  },
  {
    "id": "TV-015",
    "module": "trust-vault",
    "title": "Share URL constructed client-side using `window.location.origin` — URL varies by environment (staging vs. production, SSO proxy); links emailed to partners could be broken in production if constructed on staging",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED — verified 2026-09-02. ShareLinkModal.jsx defines APP_BASE_URL from process.env.NEXT_PUBLIC_APP_URL, falling back to window.location.origin only when unset, and both share-URL construction sites now use it instead of the raw browser origin.",
    "category": "Data Integrity",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/ShareLinkModal.jsx\n\nRoot Cause: Share URL built as `window.location.origin + '/share/' + linkToken`. If the modal is opened on a staging or preview environment, the emailed link points to staging, not production.\n\nClaude Fix Prompt: Source the base URL from environment variable: `const shareUrl = \\`\\${process.env.NEXT_PUBLIC_APP_URL}/share/\\${linkToken}\\`;`. Ensure `NEXT_PUBLIC_APP_URL` is set to the canonical production URL in all deployment environments. Render a preview of the constructed URL before sending: `<code className=\"text-xs bg-muted px-2 py-1 rounded\">{shareUrl}</code>`.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`ShareLinkModal.jsx` — `APP_BASE_URL = process.env.NEXT_PUBLIC_APP_URL || window.location.origin`, used at both share-URL construction sites, confirmed on 2026-09-02 (client-panel HEAD 0b8712ce). Assumes `NEXT_PUBLIC_APP_URL` is actually set in every deployment environment — not independently verified here (outside repo scope) — but the code-level fix is correct and complete. No regression found."
  },
  {
    "id": "TV-016",
    "module": "trust-vault",
    "title": "Expiry alert thresholds hardcoded at 7/14/30/60 days — all organisations use the same tiers; large enterprises needing 180-day warnings cannot adjust; violates ISO 27001:2022 Clause 6.1",
    "severity": "Medium",
    "status": "in_progress",
    "statusRaw": "Not implemented this pass — would need a new project-level settings collection + endpoint in trust-vault-service plus frontend wiring/UI; the backend's ExpiryAlertService already supports configurable thresholds via constructor options but nothing project-scoped exists yet. Deferred for a dedicated pass.",
    "category": "Operational Monitoring",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/ExpiryDashboard.jsx, trust-vault-service/src/jobs/expiryCheck.ts\n\nRoot Cause: Expiry alert thresholds hardcoded in both the dashboard filter chips and the cron job. Organisations needing 90-day or 180-day advance warnings (common in enterprise) cannot configure this.\n\nClaude Fix Prompt: Add org-level setting `expiryAlertThresholds: number[]` (default `[7, 14, 30, 60]`). In `expiryCheck.ts`: `const thresholds = org.settings.expiryAlertThresholds || [7, 14, 30, 60]; const certs = await CertService.getExpiringSoon(Math.max(...thresholds));`. In `ExpiryDashboard`, render filter chips from the org setting rather than hardcoded values.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-10",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Clause 6.1",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md"
  },
  {
    "id": "TV-017",
    "module": "trust-vault",
    "title": "No automated escalation or notification when certifications cross expiry thresholds — `ExpiryDashboard` is monitoring-only; no push notification, no workflow action triggered; organisations not checking daily could miss threshold crossings; violates Annex A 5.33",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "Already fixed in the live code — ExpiryAlertService.ts runs a real daily cron with per-org alert generation, 24h dedup via Redis, notification dispatch to the backend, and risk webhooks. The defect's description of 'monitoring-only, no notifications' does not match the current backend. No code change was needed; verified by reading the current file.",
    "category": "Operational Monitoring",
    "description": "Files: trust-vault-service/src/jobs/expiryCheck.ts\n\nRoot Cause: `ExpiryDashboard` is monitoring-only — it displays expiry status but triggers no push notifications, emails, or workflow actions when a threshold is crossed.\n\nClaude Fix Prompt: In `expiryCheck.ts` cron job, after identifying threshold crossings: `await NotificationService.send({ userId: cert.ownerId, type: 'cert_expiry_warning', title: \\`Certification expiring in \\${daysLeft} days\\`, body: \\`\\${cert.name} expires on \\${formatDate(cert.expiryDate)}\\`, link: \\`/trust-vault/certifications/\\${cert._id}\\` });`. Record `lastNotifiedAt` to prevent duplicate sends.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-06-12",
    "runsOpen": 1,
    "requirement": "Annex A 5.33",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md"
  },
  {
    "id": "TV-018",
    "module": "trust-vault",
    "title": "Expiry look-ahead window fixed at 90 days — `getExpiringSoon(90)` hardcoded; certifications expiring in 91+ days invisible; organisations planning 6-month renewals have no visibility",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED — verified 2026-09-02. ExpiryDashboard.jsx now has a real look-ahead Menu (30/90/180 days) driving lookAheadDays state, refetching via getExpiringSoon(lookAheadDays)/getReportsExpiringSoon(lookAheadDays) and updating the summary line. The backend's /certifications/expiring already accepted and clamped a `days` param (1-365), confirmed unchanged — no backend change was needed.",
    "category": "Operational Monitoring",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/ExpiryDashboard.jsx\n\nRoot Cause: `getExpiringSoon(90)` hardcoded. Certifications expiring in 91+ days are completely invisible in the dashboard even when organisations plan 6-month renewals.\n\nClaude Fix Prompt: Replace hardcoded `90` with a configurable look-ahead: `const lookAhead = org.settings.expiryLookAheadDays || 90; getExpiringSoon(lookAhead)`. Add a date-range picker in the dashboard header: `<Select value={lookAhead} onValueChange={setLookAhead}><SelectItem value={30}>Next 30 days</SelectItem><SelectItem value={90}>Next 90 days</SelectItem><SelectItem value={180}>Next 180 days</SelectItem></Select>`.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`ExpiryDashboard.jsx` — `LOOK_AHEAD_OPTIONS` Menu, `lookAheadDays` state wired into both API calls and the summary text — confirmed on 2026-09-02 (client-panel HEAD 0b8712ce). Backend `/certifications/expiring`'s pre-existing `expiringDays` clamp (1-365) verified still in place, so 91+ day certifications are now genuinely visible. No regression found."
  },
  {
    "id": "TV-019",
    "module": "trust-vault",
    "title": "`redactedSummary` error-prefix guard is a data corruption symptom not a fix — `TrustNetworkSharingPanel` guards `startsWith('Error')`/`startsWith('Axios')` indicating error strings persisted to DB as content; root cause in API error handler not fixed; violates ISO 27001:2022 Clause 7.5.3",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "Already fixed in the live code — trust-vault-report.routes.ts now validates redactedSummary via Zod, rejecting any value starting with Error/Axios/TypeError before it can be persisted. The frontend guard in TrustNetworkSharingPanel.jsx is a legitimate backward-compatibility filter for pre-existing corrupted records, not a live bug. No code change was needed; verified by reading both files.",
    "category": "Data Integrity",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/TrustNetworkSharingPanel.jsx\n\nRoot Cause: Guard `startsWith('Error')` / `startsWith('Axios')` in the display logic indicates that API error strings are being persisted to the database as `redactedSummary` content. The root cause in the API error handler is unfixed.\n\nClaude Fix Prompt: Fix the root cause in the Trust Vault API: wrap the `redactedSummary` generation endpoint in proper error handling — `try { const summary = await generateRedactedSummary(cert); return res.json({summary}); } catch (err) { logger.error('redactedSummary generation failed', err); return res.status(500).json({error: 'Summary generation failed'}); }`. Never persist an error string to `redactedSummary`. Remove the client-side guard once fixed.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-06-12",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Clause 7.5.3",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md"
  },
  {
    "id": "TV-020",
    "module": "trust-vault",
    "title": "No per-partner access log for Trust Network sharing — `TrustNetworkSharingPanel` shows `allowedPartnerIds` list only; no record of when each partner last accessed or how many times they downloaded; violates ISO 27001:2022 Annex A 5.14",
    "severity": "Medium",
    "status": "in_progress",
    "statusRaw": "Not implemented this pass — SharedVaultTab.jsx (initially assumed to be the partner-facing view, per TV-012) turned out to be the org's own self-preview of its shared vault, not a real cross-org partner view. Could not confidently locate where an actual partner's view event would be captured within this session's remaining scope; logging the wrong event would be worse than not logging. Deferred for a dedicated investigation.",
    "category": "Audit Trail",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/TrustNetworkSharingPanel.jsx\n\nRoot Cause: Panel shows only `allowedPartnerIds` list. No record of when each partner last accessed the shared certification or how many times they downloaded it.\n\nClaude Fix Prompt: Backend: log `{partnerId, certId, accessedAt, action: 'view'|'download'}` on each Trust Network access. Frontend `TrustNetworkSharingPanel`: add `<AccessLogTable partnerId={p.id} certId={certId} />` expandable per partner row showing their access history.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-10",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Annex A 5.14",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md"
  },
  {
    "id": "TV-021",
    "module": "trust-vault",
    "title": "Draft certifications can be mapped to compliance frameworks before validation — `CertFrameworkMappingPanel` available regardless of cert status; draft certs contribute to framework coverage score; inflates coverage metrics with unvalidated evidence; violates ISO 27001:2022 Clause 7.5.2",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED (backend guard) — verified 2026-09-02. `CertFrameworkMappingPanel.jsx` confirmed dead code (zero import sites anywhere in the app), so no frontend change was warranted. `framework-mapping.routes.ts`'s POST handler now looks up the certification's status and rejects (400) any mapping attempt against a draft cert — this closes the real gap regardless of which frontend (if any) calls it.",
    "category": "Data Validation",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/CertFrameworkMappingPanel.jsx\n\nRoot Cause: `CertFrameworkMappingPanel` renders and accepts mappings regardless of certification `status`. Draft certificates contribute to framework coverage scores before any validation has occurred.\n\nClaude Fix Prompt: In `CertFrameworkMappingPanel`: `if (cert.status === 'draft') return (<Alert>Framework mapping is only available for validated certifications. Complete the review process first.</Alert>);`. Backend framework mapping creation endpoint: `if (cert.status === 'draft') return res.status(400).json({error: 'Cannot map draft certifications to frameworks'});`.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Clause 7.5.2",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`trust-vault-service/src/routes/framework-mapping.routes.ts:161-175` — looks up `COLLECTIONS.CERTIFICATIONS` by `certificationId`+org+isLatest, returns 400 if `status === 'draft'`, explicitly citing TV-021. Confirmed present on 2026-09-02 (trust-vault-service HEAD 12b4103). The real, reachable live page (`app/(private)/trust-vault/framework-mappings/page.jsx`) surfaces this 400 cleanly via its existing catch-block toast, so no new crash/blank-error regression. No regression found."
  },
  {
    "id": "TV-022",
    "module": "trust-vault",
    "title": "Partial-scope certification mappings not disclosed to partners or in public share views — partner may assume full ISO 27001 coverage when only 5 specific controls are covered; violates ISO 27001:2022 Clause 7.5.1",
    "severity": "Medium",
    "status": "in_progress",
    "statusRaw": "Skipped per user decision alongside TV-021 — the certification-to-framework mapping feature this describes has no live UI today (CertFrameworkMappingPanel.jsx is unreachable), so there is no screen to add a partial-scope disclosure banner to.",
    "category": "Data Completeness",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/TrustNetworkSharingPanel.jsx\n\nRoot Cause: When a certification covers only a partial scope (e.g., 5 specific Annex A controls rather than the full standard), this limitation is not surfaced to partners in the Trust Network shared view or share link view.\n\nClaude Fix Prompt: In the shared certification view: `{cert.scope === 'partial' && <Alert variant=\"warning\"><AlertTitle>Partial Scope</AlertTitle>This certification covers: {cert.scopeDescription}. It does not represent full compliance with {cert.framework}.</Alert>}`. Add `scope` and `scopeDescription` fields to the certification schema if not already present.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-10",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Clause 7.5.1",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md"
  },
  {
    "id": "TV-023",
    "module": "trust-vault",
    "title": "No cryptographic hash or tamper-evident seal per certification version — `VersionHistory` shows version/changeType/changedBy/timestamp only; no SHA-256 hash stored or displayed; chain-of-custody cannot be verified; violates ISO 27001:2022 Clause 7.5.3",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED (backend) — verified 2026-09-02. `VersionHistory.jsx` confirmed dead code (zero import sites), so correctly no frontend display was added. `CertificationRepository.ts` now has a real `computeContentHash()` (SHA-256 over title/description/frameworkId/frameworkVersion/issueDate/expiryDate/issuingAuthority/files), called and stored as `contentHash` in both `create()` and `createVersionedUpdate()` — the tamper-evident data now genuinely exists on every version, ready to surface whenever this feature gets a live screen.",
    "category": "Data Integrity",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/VersionHistory.jsx, trust-vault-service/src/services/CertificationService.ts\n\nRoot Cause: `VersionHistory` shows version number, change type, changed by, and timestamp only. No SHA-256 hash of the document content is computed on upload or stored/displayed per version.\n\nClaude Fix Prompt: Backend `CertificationService.createVersion`: `const hash = crypto.createHash('sha256').update(JSON.stringify(versionContent)).digest('hex'); version.contentHash = hash;`. Frontend `VersionHistory`: `<code className=\"text-xs font-mono text-muted-foreground\">{version.contentHash?.slice(0, 16)}…</code>` with copy-to-clipboard button. Add `Copy full hash` link for audit packages.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Clause 7.5.3",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`trust-vault-service/src/repositories/CertificationRepository.ts` — `computeContentHash()` (new, imports `crypto`), called in both `create()` and `createVersionedUpdate()`, storing `doc.contentHash`/`newDoc.contentHash`, confirmed on 2026-09-02 (trust-vault-service HEAD 12b4103). Genuinely different from TV-011's situation: here the backend data model itself is the fix, and it's real and complete even though the display layer is correctly deferred (no live screen exists yet). No regression found."
  },
  {
    "id": "TV-024",
    "module": "trust-vault",
    "title": "Three parallel workflow state maps inconsistent across components — `CERT_TRANSITIONS`, `CERT_STATUS_ACTIONS`, `STATUS_CONFIG` define lifecycle in three separate places with divergences; future changes risk further inconsistency; violates ISO 27001:2022 Clause 7.5",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED (partial consolidation, judged sufficient) — verified 2026-09-02. Confirmed the 3 maps were not actually silently diverged (a re-check, not just an assumption). `CERT_ALLOWED_TRANSITIONS` extracted as the canonical source in constants.js, mirroring the backend's WorkflowTransitionService.getAllowedTransitions; CertificationDetails.jsx's own CERT_TRANSITIONS now derives from it (REVIEWER_ONLY_STATES blanking). WorkflowActionPanel.jsx's CERT_STATUS_ACTIONS kept its own action-keyed shape (legitimately different — needs labels/icons/colors) with a cross-reference comment added.",
    "category": "Workflow Integrity",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/WorkflowActionPanel.jsx\n\nRoot Cause: `CERT_TRANSITIONS`, `CERT_STATUS_ACTIONS`, and `STATUS_CONFIG` each define parts of the certification lifecycle independently and have diverged (TV-004 is one consequence). Any future lifecycle change risks further inconsistency.\n\nClaude Fix Prompt: Create a single source of truth: `CERTIFICATION_LIFECYCLE` object: `{ draft: { label: 'Draft', color: 'gray', actions: ['submit_for_review'] }, under_review: { ... }, active: { ... }, expiring_soon: { ... }, expired: { ... }, revoked: { ... } }`. Derive all three current maps from this single config at module load time.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Clause 7.5",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`constants.js` — `CERT_ALLOWED_TRANSITIONS` (canonical) + `CertificationDetails.jsx`'s `CERT_TRANSITIONS` now `Object.fromEntries`-derived from it — confirmed on 2026-09-02 (client-panel HEAD 0b8712ce). **Residual gap, disclosed and reasonable, not blocking:** full single-source unification of all 3 maps (including `WorkflowActionPanel`'s differently-shaped `CERT_STATUS_ACTIONS`) was not done — a legitimate scope trade-off given the shape mismatch, not neglect. Reduces the defect from 'three independently-maintained sources' to 'two, with one now provably derived from the other and no confirmed current divergence.' No regression found."
  },
  {
    "id": "TV-025",
    "module": "trust-vault",
    "title": "Audit log event coverage undocumented and completeness unverifiable — `CertificationDetails` shows audit log but no documentation of which events are captured; auditor cannot verify completeness of evidence trail; violates ISO 27001:2022 Annex A 8.15",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED with an accurate tooltip — verified 2026-09-02. An info icon next to the Workflow History event count in CertificationDetails.jsx now describes the events actually captured (every workflow status transition with actor/timestamp/note), correctly NOT reusing the fabricated event list from the original fix prompt (note_added, framework_mapped etc. do not exist in workflow_entries).",
    "category": "Audit Trail",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/CertificationDetails.jsx\n\nRoot Cause: `CertificationDetails` displays an audit log but provides no documentation of which events are captured, which actors can generate them, or whether the log is complete. An auditor cannot verify the trail is exhaustive.\n\nClaude Fix Prompt: Add an info tooltip on the audit log heading: `<Tooltip content=\"Events captured: view, status_transition, reviewer_assigned, reviewer_submitted, note_added, framework_mapped, framework_archived, share_link_created, trust_network_enabled. Document downloads logged separately in access log.\"><InfoIcon /></Tooltip>`. Ensure all events listed in the tooltip are actually emitted in the backend.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "requirement": "ISO 27001:2022 Annex A 8.15",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`CertificationDetails.jsx` — info-icon `Tooltip` next to the audit-log event count, text reading 'Events captured: every workflow status transition..., with actor, timestamp, and any note attached. File uploads, share-link activity, and framework mappings are tracked separately and not shown in this timeline' — confirmed accurate against the real backend behavior on 2026-09-02 (client-panel HEAD 0b8712ce). Higher quality than the fix prompt asked for: the prompt's suggested tooltip text listed events that don't actually exist in `workflow_entries`, which this implementation correctly avoided. No regression found."
  },
  {
    "id": "TV-026",
    "module": "trust-vault",
    "title": "ISO 27001 version (2013 vs 2022) never shown — `FRAMEWORK_COLORS = { 'ISO27001': 'blue' }` single entry; all ISO 27001 certs appear identical; mandatory 2013→2022 transition deadline passed; auditor cannot verify edition compliance; violates Clause 7.5.1",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED end-to-end — verified 2026-09-02, full chain traced. frameworkVersion flows: UploadCertificationModal.jsx (new input, registered in the yup schema, spread into the onSubmit payload) -> certification.routes.ts (zod schema on create+update) -> CertificationService.ts (interfaces + DTO passthrough) -> CertificationRepository.ts (persisted on create + createVersionedUpdate) -> useTrustVault.js mapper -> CertificationDetails.jsx (versioned FRAMEWORK_COLORS badge + DEPRECATED_FRAMEWORK_VERSIONS warning badge). A same-day follow-up commit fixed a TypeScript build error the initial change introduced (missing field on two request interfaces) — a caught-and-fixed build issue, not a shipped regression.",
    "category": "ISO Compliance",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/constants.js\n\nRoot Cause: `FRAMEWORK_COLORS = { 'ISO27001': 'blue' }` — single entry treating all ISO 27001 certifications as equivalent. The mandatory 2013→2022 transition deadline has passed and editions are architecturally different.\n\nClaude Fix Prompt: Expand to: `FRAMEWORK_COLORS = { 'ISO27001:2013': 'blue', 'ISO27001:2022': 'indigo', 'ISO27001': 'blue' }`. Update certification schema to store `frameworkVersion: '2013' | '2022'`. Display badge in certification cards: `<Badge>{cert.framework} {cert.frameworkVersion}</Badge>`. Flag 2013 certs with a deprecation warning.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "requirement": "Clause 7.5.1",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "Full chain confirmed on 2026-09-02: `UploadCertificationModal.jsx` (input + `onSubmit({...data, ...rcMappings})` spread) → `certification.routes.ts` (zod `frameworkVersion` on create/update/transition-adjacent schemas) → `CertificationService.ts`/`CertificationRepository.ts` (DTOs + `computeContentHash` inputs + persisted field on create/version) → `useTrustVault.js:40` (`frameworkVersion: data.frameworkVersion || undefined`) → `constants.js` (`FRAMEWORK_COLORS['ISO27001:2013']`/`':2022'`, `DEPRECATED_FRAMEWORK_VERSIONS`) → `CertificationDetails.jsx` (version-aware badge + 'Deprecated Edition' tooltip badge). Client-panel HEAD 0b8712ce, trust-vault-service HEAD 12b4103 + follow-up commit 1396609 (same-day TS interface fix). No regression found."
  },
  {
    "id": "TV-027",
    "module": "trust-vault",
    "title": "Entire Trust Vault gated on single permission — no sub-feature RBAC; `WorkflowActionPanel` does not filter by role; any `trust_vault.write` user can revoke; four-eyes principle unenforceable; violates ISO 27001:2022 Annex A 5.3",
    "severity": "Medium",
    "status": "in_progress",
    "statusRaw": "NOT FIXED (self-disclosed no-op, confirmed 2026-09-02) — WorkflowActionPanel.jsx does now gate the Revoke action behind usePermission('trust_vault.approve'), but trust-vault-service's POST /certifications/:id/transition route requires TRUST_VAULT_PERMISSIONS.APPROVE uniformly for EVERY target state, not just revoke — confirmed directly in certification.routes.ts's preHandler. Anyone who can already approve/close/assign already holds trust_vault.approve, so this frontend check restricts nobody who wasn't already restricted. The actual ask — a distinct trust_vault.revoke permission enforced server-side so approve-only users cannot revoke — was not implemented, exactly as the fix's own caveat predicted. Flipped back to open rather than left in_progress or marked fixed.",
    "category": "Access Control",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/WorkflowActionPanel.jsx\n\nRoot Cause: All Trust Vault features sit behind a single `trust_vault.write` permission. `WorkflowActionPanel` does not differentiate by action type — any `trust_vault.write` user can revoke certifications.\n\nClaude Fix Prompt: Add sub-permissions: `trust_vault.review` (submit reviews), `trust_vault.approve` (close/approve), `trust_vault.revoke` (revoke). In `WorkflowActionPanel`: `if (action.targetState === 'revoked' && !hasPermission('trust_vault.revoke')) return null;`. Backend: enforce sub-permissions on transition endpoints.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-10",
    "runsOpen": 2,
    "requirement": "ISO 27001:2022 Annex A 5.3",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`WorkflowActionPanel.jsx:196,207` — `canRevoke = usePermission('trust_vault.approve')` filters the revoke action, confirmed present. But `trust-vault-service/src/routes/certification.routes.ts:415-419` — `preHandler: [augmentPermissionsWithProjectGrant(...), requirePermission(TRUST_VAULT_PERMISSIONS.APPROVE)]` on `POST /certifications/:id/transition` — confirms this same permission already gates every transition, not just revoke, making the new frontend check a no-op in practice. No distinct `trust_vault.revoke` permission was created and no backend permission-by-targetState logic was added. Verdict: not genuinely fixed — segregation of duties for revocation remains unenforceable, as the fix's own author explicitly flagged."
  },
  {
    "id": "TV-028",
    "module": "trust-vault",
    "title": "Vault Health grade formula undisclosed — `VaultHealthWidget` shows score and A–F grade with no tooltip, methodology explanation, or sub-dimension weighting; management review cannot cite unexplained automated grade; violates ISO/IEC 27007 §6.4.9",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED with an accurate tooltip — verified 2026-09-02. An info icon next to the Grade badge (both compact and full views) in VaultHealthWidget.jsx now describes the real weighting formula, cross-checked line-by-line against VaultHealthService.ts's actual scoring code — not a guessed one.",
    "category": "Audit Trail",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/VaultHealthWidget.jsx\n\nRoot Cause: `VaultHealthWidget` shows a score (e.g., 73) and an A–F grade with no tooltip, methodology explanation, or sub-dimension weighting. Management review cannot cite an unexplained automated grade.\n\nClaude Fix Prompt: Add methodology tooltip: `<Tooltip content=\"Vault Health = (Active Certs 40%) + (No Expiring Soon 25%) + (All Reviews Complete 20%) + (Trust Network Active 15%). Grade: A=90+, B=75+, C=60+, D=45+, F=<45\"><InfoIcon className=\"w-4 h-4 inline ml-1 text-muted-foreground\" /></Tooltip>`. Link to a \"How is this calculated?\" documentation page.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "requirement": "ISO/IEC 27007 §6.4.9",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`VaultHealthWidget.jsx` — `HEALTH_METHODOLOGY` tooltip text checked word-for-word against `trust-vault-service/src/services/VaultHealthService.ts`'s real scoring logic (certifications +40/+20/+20/+20, reports same pattern, recency 100 minus 15/expired minus 5/expiring, workflow 50 plus 20/plus 10 minus 30-or-15 plus 20, grade bands A=90/B=80/C=70/D=60/F=<60) — every number matches exactly. Confirmed on 2026-09-02 (client-panel HEAD 0b8712ce, trust-vault-service HEAD 12b4103). Notably better than the fix prompt's own suggested tooltip text, which described a different, incorrect formula. No 'How is this calculated?' documentation link was added (minor residual gap). No regression found."
  },
  {
    "id": "TV-029",
    "module": "trust-vault",
    "title": "No incident severity escalation path — `IncidentForm` severity field has no SLA timer or automated escalation when `severity = 'critical'` remains `status = 'open'` beyond threshold; critical incident open 72h looks identical to one created 5 minutes ago; violates Annex A 5.26",
    "severity": "Medium",
    "status": "fixed",
    "statusRaw": "FIXED — verified 2026-09-02, real cron job confirmed registered and running. A new hourly 'incident-escalation' job in CertificationJobsManager (expiryCheck.ts) notifies the reporter of any critical incident open/investigating past 72h (escalatedAt stamped to prevent re-notification, added to incident.schema.ts). A matching SLA badge ('SLA: Xh left' / 'SLA BREACHED') was added to IncidentCard.jsx, the live incidents list component. Notifies the incident's creator rather than a 'security lead' role, since no such role/assignment field exists on incidents today — a disclosed, reasonable substitution.",
    "category": "Data Validation",
    "description": "Files: vibrantix-client-panel/src/components/trust-vault/IncidentForm.jsx, trust-vault-service/\n\nRoot Cause: `severity` field has no SLA timer or automated escalation when a critical severity incident remains `status: 'open'` beyond a threshold (e.g., 72 hours). A critical incident looks identical to one created 5 minutes ago.\n\nClaude Fix Prompt: Backend job: `IncidentEscalationJob` — daily query for `{severity: 'critical', status: {$in: ['open','investigating']}, createdAt: {$lt: subHours(new Date(), 72)}}`. For each: send escalation notification to CISO/security lead role. Update `escalatedAt`. Frontend: display SLA countdown on critical open incidents: `<Badge variant=\"destructive\">SLA: {hoursRemaining}h remaining</Badge>`.",
    "firstDetected": "2026-05-20",
    "lastVerified": "2026-09-02",
    "runsOpen": 1,
    "requirement": "Annex A 5.26",
    "sourceFile": "QA_Tracker/TRUST_VAULT_QA_LIVE_TRACKER.md",
    "fixEvidence": "`trust-vault-service/src/jobs/expiryCheck.ts` — `runIncidentEscalation()` (hourly, `INCIDENT_ESCALATION_HOURS=72`, queries severity:critical + status open/investigating + createdAt<cutoff + no escalatedAt yet, sends assignment notification, stamps escalatedAt) + `src/models/incident.schema.ts` (`escalatedAt?: Date`) + `src/index.ts:338-342` (job registered via `initializeJobsManager`, `enableIncidentEscalation` defaults true since the option isn't explicitly disabled) — confirmed the job genuinely starts, not just defined. Frontend `IncidentCard.jsx` — SLA badge mirroring the same 72h threshold, confirmed on 2026-09-02 (client-panel HEAD 0b8712ce, trust-vault-service HEAD 12b4103). No regression found."
  },
  {
    "id": "TV-CHK-001",
    "module": "trust-vault",
    "title": "Two of the module's three built email notification templates — `share_link_created` and `certification_uploaded` — are fully rendered/wired functions with zero callers anywhere in the request-handling code; only the expiry-alert path (a different, scheduled-job pipeline) actually fires",
    "severity": "Medium",
    "status": "new",
    "statusRaw": "🆕 NEW",
    "category": "Checklist §5.1 — Notifications & Real-Time Updates",
    "affectedFiles": [
      "trust-vault-service/src/services/EmailTemplateService.ts:120-141",
      "trust-vault-service/src/templates/emails/share_link_created.hbs",
      "trust-vault-service/src/templates/emails/certification_uploaded.hbs"
    ],
    "firstDetected": "2026-09-10",
    "lastVerified": "2026-09-10",
    "runsOpen": 1,
    "sourceFile": "Architecture/MODULE_FEATURE_CHECKLIST.md",
    "description": "**Checklist row:** §5.1 — Key lifecycle events dispatch a notification: assignment, due-date approaching/overdue, approval requested, approval decision, status change — each fires through the standard dispatch pipeline.\n**Files:** `EmailTemplateService.ts` exports `renderShareLinkCreatedEmail` (line 134-141, renders the `share_link_created` template) and `renderCertificationUploadedEmail` (line 120-128, renders `certification_uploaded`) — both fully implemented. A repo-wide grep for either function name across `trust-vault-service/src/` returns matches only in `EmailTemplateService.ts` itself and the `services/index.ts` barrel re-export — no route, controller, or job ever calls either one. By contrast, the module's third template (`certification_expiry_alert`) IS genuinely wired: `ExpiryAlertService.ts`'s scheduled job calls `sendToBackend()` to dispatch a real notification for expiring certifications (independently confirmed working, per this module's own `TV-*` fix history for incident escalation using the same job-based pattern).\n**Root Cause:** The email templates and rendering functions for 'a share link was created' and 'a certification was uploaded' were built but the actual call sites — in the share-link creation route (`trust-vault-share-link.routes.ts`) and the certification upload route — were never added.\n**Impact:** No one is notified when a certification is uploaded (e.g. a compliance manager wouldn't know a new certification needs their review) or when a share link is created for their organization's certification (e.g. an admin auditing outbound sharing has no proactive signal, only the reactive per-access audit trail already tracked separately in this module's existing `TV-*` records). This is dead-but-functional code sitting one call site away from working — a materially cheaper fix than most notification gaps found this session, since the render/template layer is already done.\n**Claude Fix Prompt:**\nIn the share-link creation handler (`trust-vault-share-link.routes.ts`'s `POST /certifications/:id/share-links`-equivalent route — find via grep for where `ShareLink` documents are created), call `EmailTemplateService.renderShareLinkCreatedEmail({...})` and dispatch it via the same `BackendApiClient`/`sendToBackend`-style path `ExpiryAlertService` already uses. Do the same for the certification upload route, calling `renderCertificationUploadedEmail`. Both functions already have the right signature — this is purely a missing invocation, not new logic to write."
  }
]
